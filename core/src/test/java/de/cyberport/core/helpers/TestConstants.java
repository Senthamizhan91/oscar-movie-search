package de.cyberport.core.helpers;

import java.io.File;

public class TestConstants {
    public static final String USER_DIR = System.getProperty("user.dir");
    public static final String RESOURCES_FOLDER = USER_DIR + File.separator + "src" + File.separator + "test" + File.separator + "resources";
}
